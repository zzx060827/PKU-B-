#include <stdio.h>
#include <string.h>

int main() 
{
    int n;scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        char a[100]={0},b[100]={0},c[100]={0},d[100]={0};
        scanf("%s.%s",a,b);scanf("%s.%s",c,d);
        int s2;
        s2++;
        
    }
}